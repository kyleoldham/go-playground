package main

func dfs(Node root) {
	//Queue creation

	for node.next == true {
		Node = queue.pop()
		visit(r)
		for Node.Left && Node.Right {
			if n.marked == false {
				n.marked = true
				queue.enqueue(n)
			}
		}
	}
}
